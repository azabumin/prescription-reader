// Google AdSense is disabled until both values below are filled in.
// You must register the site with AdSense yourself (console.adsense.google.com) —
// this can't be done on your behalf. Once approved:
//   1. Paste your publisher ID (format: "ca-pub-XXXXXXXXXXXXXXXX") into ADSENSE_CLIENT_ID.
//   2. Create an ad unit in the AdSense dashboard and paste its slot ID into ADSENSE_SLOT_ID.
//   3. Redeploy (npm run deploy). The ad slot on the result screen will start rendering.
//
// ads.txt note: because this app is served under azabumin.github.io/prescription-reader/
// (a project path, not the domain root), AdSense's required ads.txt file must live at
// https://azabumin.github.io/ads.txt — that's controlled by whatever repo serves the
// account-level azabumin.github.io site, not this repo. Check that separately.
export const ADSENSE_CLIENT_ID = '';
export const ADSENSE_SLOT_ID = '';
